
{

  servers = [
    {
      hostname = "host01.example.com"
      tls = {
        files = {
          keyfile = "/etc/ssl/certs/host01.example.com.key"
          certfile = "/etc/ssl/certs/host01.example.com.crt"
        }
      }
      backend = {
        url = "http://localhost:8080"
      }
    }

    {
      hostname = "host02.example.com"
      tls = {
        files = {
          keyfile = "/etc/ssl/certs/host02.example.com.key"
          certfile = "/etc/ssl/certs/host02.example.com.crt"
        }
      }
      backend = {
        url = "http://localhost:8080"
      }
    }
  ]
}
